import * as internal from "stream";

export class ipodetails{
    constructor(
        companyname:string,
        stockexchange:string,
        pricepershare:number,
        totalshare:number,
        opendate:string,
        remarks:string
    ){}
}